--- a/app/ai/system_prompt.py
+++ b/app/ai/system_prompt.py
@@ -21,6 +21,13 @@
 create_job вместо попытки сделать всё немедленно за один диалог.
 Для запросов про свежие/актуальные события учитывай сегодняшнюю дату и проверяй даты
 источников, которые находишь.
+
+Для динамических сайтов и маркетплейсов, особенно avito.ru, используй браузер Playwright.
+Если задача связана с объявлениями, ценами, фильтрами, входом в аккаунт, чатами,
+сообщениями, кнопками или формами на Avito — НЕ используй fetch_page как основной способ:
+сначала browser_open, затем browser_read и при необходимости browser_click/browser_type.
+fetch_page не исполняет JavaScript и может не видеть реальные цены/объявления.
+Если пользователь дал ссылку avito.ru, открывай именно её через browser_open.
 
 Ты умеешь выполнять составные цели из нескольких шагов подряд, самостоятельно решая
 после каждого результата инструмента, какой шаг делать дальше. Не завершай задачу, пока
--- a/app/tools/fetch_page.py
+++ b/app/tools/fetch_page.py
@@ -61,8 +61,10 @@
 class FetchPageTool(Tool):
     name = "fetch_page"
     description = (
-        "Загружает веб-страницу по URL и возвращает её основной текст (обрезанный по объёму). "
-        "Используй для чтения конкретной страницы, найденной через web_search."
+        "Загружает статический HTML по URL и возвращает основной текст. "
+        "Не исполняет JavaScript. Не используй как основной инструмент для Avito, "
+        "маркетплейсов, личных кабинетов, чатов, динамических цен и интерактивных страниц — "
+        "для них используй browser_open/browser_read."
     )
     args_schema = FetchPageArgs
     permission = PermissionLevel.SAFE
--- a/app/tools/browser_open.py
+++ b/app/tools/browser_open.py
@@ -13,7 +13,11 @@
 
 class BrowserOpenTool(Tool):
     name = "browser_open"
-    description = "Открывает страницу по URL в headless-браузере."
+    description = (
+        "Открывает страницу в реальном Chromium/Playwright с сохранённой пользовательской "
+        "сессией. Предпочитай этот инструмент для Avito и других динамических сайтов, "
+        "где нужны JavaScript, актуальные цены, авторизация, кнопки, формы или чаты."
+    )
     args_schema = BrowserOpenArgs
     permission = PermissionLevel.SAFE
 
